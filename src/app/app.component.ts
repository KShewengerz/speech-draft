import { Component } from '@angular/core';


/**
 * A component that holds and renders its child components' template.
 */
@Component({
  selector: 'app-root',
  template: `<router-outlet></router-outlet>`
})
export class AppComponent {}
